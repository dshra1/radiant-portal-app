# Copy to .env for local dev. In production (Vercel), set these as
# Environment Variables in the Vercel project dashboard instead — never
# commit real secret keys to git.

# --- Supabase (existing project, unchanged) ---
SUPABASE_PROJECT_ID="hnwxfslyyxjhblqzretu"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
SUPABASE_URL="https://hnwxfslyyxjhblqzretu.supabase.co"
VITE_SUPABASE_PROJECT_ID="hnwxfslyyxjhblqzretu"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
VITE_SUPABASE_URL="https://hnwxfslyyxjhblqzretu.supabase.co"

# SECRET — server-side only, bypasses RLS. Get from Supabase dashboard >
# Project Settings > API > service_role key. Set this ONLY in Vercel's
# environment variables, never in a committed .env file.
SUPABASE_SERVICE_ROLE_KEY=""

# --- AI (replaces Lovable's AI gateway) ---
# SECRET. Get a free/pay-as-you-go key at https://aistudio.google.com/apikey
# Set this ONLY in Vercel's environment variables, never in a committed
# .env file — this key is directly billable per request.
GOOGLE_GENERATIVE_AI_API_KEY=""
